{
  "schemaVersion": "1.0.0",
  "name": "automate-monthly-expenses",
  "version": "1.0.0",
  "description": "Reusable skill module for deploying a Google Sheets-backed monthly expense automation website.",
  "entrypoints": {
    "instructions": "SKILL.md",
    "apiSpec": "openapi.yaml"
  },
  "inputs": {
    "expense": {
      "type": "object",
      "required": ["date", "category", "amount", "paymentMethod"],
      "properties": {
        "date": { "type": "string", "format": "date" },
        "category": { "type": "string" },
        "amount": { "type": "number", "exclusiveMinimum": 0 },
        "fee": { "type": ["number", "string"], "description": "Number for transfer fee, or empty string when not applicable." },
        "paymentMethod": { "type": "string" },
        "bankCardName": { "type": "string" },
        "note": { "type": "string" }
      }
    },
    "month": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}$"
    }
  },
  "outputs": {
    "monthlySummary": {
      "type": "object",
      "properties": {
        "month": { "type": "string" },
        "total": { "type": "number" },
        "currency": { "type": "string" },
        "count": { "type": "integer" },
        "highExpenseCount": { "type": "integer" },
        "categories": { "type": "array" }
      }
    },
    "monthlyReport": {
      "type": "object",
      "properties": {
        "month": { "type": "string" },
        "reportSheetName": { "type": "string" },
        "total": { "type": "number" },
        "categories": { "type": "array" },
        "updatedAt": { "type": "string" }
      }
    }
  }
}
